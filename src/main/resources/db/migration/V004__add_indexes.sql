CREATE INDEX t_vocabularies_study_index ON t_vocabularies (study);

CREATE INDEX t_vocabularies_created_at_index ON t_vocabularies (created_at);

CREATE INDEX t_vocabularies_created_at_desc_index ON t_vocabularies (created_at DESC);
